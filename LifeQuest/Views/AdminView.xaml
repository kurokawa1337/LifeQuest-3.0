<UserControl x:Class="LifeQuest.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BgBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32,28">
        <StackPanel>
            <TextBlock x:Name="TitleText" Style="{StaticResource H1}"/>
            <TextBlock x:Name="SubText" Style="{StaticResource Muted}" Margin="0,2,0,0"/>
            <ItemsControl x:Name="QueueList" Margin="0,20,0,0"/>
            <TextBlock x:Name="EmptyText" Style="{StaticResource Muted}" Margin="0,20,0,0" Visibility="Collapsed"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
